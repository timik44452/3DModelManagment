<UserControl x:Class="Manager.CustomControls.ModelViewLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Manager.CustomControls"
             xmlns:sys="clr-namespace:System;assembly=System"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.1*"/>
            <ColumnDefinition Width="7"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Ellipse Grid.Column="1" Width="5" Height="5" Fill="Red"/>
        <Label Grid.Column="2" Content="{Binding Name}" VerticalContentAlignment="Center"/>
        <Label Grid.Column="4" Content="{Binding Date,Mode = OneWay, StringFormat='yyyy-MM-dd HH:mm:ss '}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
    </Grid>
</UserControl>
